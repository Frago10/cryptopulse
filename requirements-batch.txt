# =============================================================================
# CryptoPulse — Dependencias MÍNIMAS para Fase 1 (batch ingestion)
# Esta lista es chica a propósito: evita confluent-kafka (compila nativo en
# Windows y rompe) y paquetes ML pesados. Instalar esto es rápido (~1 min).
#
#   pip install -r requirements-batch.txt
# =============================================================================

# Core
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4
structlog==24.1.0

# Datos
pandas==2.2.2
pyarrow==16.1.0
numpy==1.26.4

# Ingesta batch
requests==2.32.3
praw==7.7.1

# Storage (MinIO vía S3 API)
boto3==1.34.80

# Tests
pytest==8.2.2
pytest-asyncio==0.23.7
