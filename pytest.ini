[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: tests que requieren servicios levantados (docker compose up)
addopts = -ra --strict-markers
